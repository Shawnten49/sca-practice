# ShardingSphere 5.5.x Seata AT 集成配置（org.apache.seata.config.FileConfiguration 格式）
shardingsphere.transaction.seata.at.enable = true
shardingsphere.transaction.seata.tx.timeout = 60

client {
    application.id = order-service
    transaction.service.group = seata_tx_group
}
