<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.task.mapper.ProductMapper">

    <select id="selectRecentByShard" resultType="com.example.dto.ProductDTO">
        SELECT id, name, brand, price, description, create_time
        FROM product
        WHERE create_time &gt;= #{cutoff}
          AND MOD(id, #{shardTotal}) = #{shardIndex}
          AND id &gt; #{lastId}
        ORDER BY id ASC
        LIMIT #{batchSize}
    </select>

</mapper>
